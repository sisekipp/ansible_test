---
  - hosts: localhost
    gather_facts: false

    tasks:
    - name: Print Message
      debug:
        msg: Hello from Ansible World!
    - name: Pause for 2 minutes
      ansible.builtin.pause:
        seconds: 10
    
